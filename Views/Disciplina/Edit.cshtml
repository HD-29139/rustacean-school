@model Disciplina

@{
    ViewData["Title"] = "Edit Page";
}

<div class="text-center">
    <h1 class="rust-font">Editar Disciplina</h1>
    <form asp-controller="Disciplina" asp-action="Edit" method="post">
        <div class="mb-3">
            <label for="nome" class="form-label">Nome da Disciplina</label>
            <input type="text" asp-for="Nome" pattern="[A-Za-zÀ-ÿ\s]{3,35}" minlength="3" maxlength="35"
                title="DIGITE APENAS LETRAS E ESPAÇOS!! min=3 max=35" class="form-control" id="nome" placeholder="Nome"
                required>
            <span asp-validation-for="Nome" class="text-danger"></span>
            <button type="submit" class="botaoCriar">Editar</button>
        </div>
        <input type="hidden" asp-for="DisciplinaID">
    </form>


</div>
